{
  "url": "/getExpensePolicies",
  "method": "GET",
  "qs": {
    "ResultsPerPage": 100,
    "Page": "{{pagination.page}}"
  },
  "response": {
    "iterate": "{{body.ExpensePolicies}}",
    "output": {
      "label": "{{item.Name || item.Id}}",
      "value": "{{item.Id}}"
    }
  },
  "pagination": {
    "condition": "{{body.Records.Page < body.Records.Pages}}",
    "qs": {
      "Page": "{{pagination.page}}"
    }
  }
}
