{
  "url": "/getExpensePolicyTaxes",
  "method": "GET",
  "qs": {
    "IdPolicy": "{{parameters.IdPolicy}}"
  },
  "response": {
    "iterate": "{{body.Taxes}}",
    "output": "{{item}}",
    "limit": "{{parameters.limit}}"
  }
}
