{
  "url": "/sandbox",
  "method": "GET",
  "qs": {
    "limit": 100
  },
  "response": {
    "iterate": "{{body.data || body.items || body}}",
    "output": {
      "label": "{{item.name || item.id}}",
      "value": "{{item.id || item.name}}"
    }
  }
}
