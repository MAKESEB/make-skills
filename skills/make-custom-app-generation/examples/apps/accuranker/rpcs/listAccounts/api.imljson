{
  "url": "/accounts/",
  "method": "GET",
  "qs": {
    "fields": "id,name",
    "limit": 100
  },
  "response": {
    "iterate": "{{body.results || body}}",
    "output": {
      "label": "{{item.name || item.id}}",
      "value": "{{item.id}}"
    }
  }
}
