{
  "url": "/domains/",
  "method": "GET",
  "qs": {
    "fields": "{{parameters.fields}}",
    "limit": "{{parameters.limit}}",
    "offset": "{{parameters.offset}}"
  },
  "response": {
    "iterate": "{{body.results || body}}",
    "output": "{{item}}"
  }
}
