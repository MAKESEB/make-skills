{
  "url": "/akten/{{parameters.laufendeNummer}}/{{parameters.bezugsJahr}}/ordner",
  "method": "GET",
  "response": {
    "iterate": "{{body.content || body}}",
    "output": {
      "label": "{{item.name}}",
      "value": "{{item.id}}"
    }
  }
}
