{
  "url": "/akten/{{parameters.laufendeNummer}}/{{parameters.bezugsJahr}}/honorar",
  "method": "GET",
  "response": {
    "iterate": "{{values(body.mandanten)}}",
    "output": {
      "label": "{{item.anzeigename}}",
      "value": "{{item.id}}"
    }
  }
}
